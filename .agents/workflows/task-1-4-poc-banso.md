---
description: Task 1.4 - PoC bản đồ Leaflet (hạ rủi ro kỹ thuật)
---

# Workflow: Task 1.4 — PoC bản đồ Leaflet
### 1. Chuẩn bị
- Chạy `/mo-phien` trước (nạp ngữ cảnh, xác định vai, chờ tôi duyệt).
- Đóng vai EXECUTOR.

### 2. Viết 1 file HTML độc lập (không cần Django)
- Bản đồ Leaflet + OSM canh giữa Đà Nẵng.
- 5 marker sân mẫu (tọa độ [GIẢ ĐỊNH], ghi rõ dữ liệu giả).
- Mỗi marker popup: tên, địa chỉ, giá, nút "Đặt sân" (alert demo).
- Nút "Sân gần tôi": lấy vị trí trình duyệt, tính Haversine, highlight sân gần nhất.
- Attribution OpenStreetMap đúng ODbL.

### 3. Giải thích
- Hướng dẫn chạy + giải thích từng phần để tôi tự bảo vệ.
### Kết thúc
- Chạy `/dong-phien` (cập nhật STATE, tick ROADMAP, ghi EVIDENCE, nhắc commit).

## Tiêu chí hoàn thành
File chạy được; chụp màn hình lưu EVIDENCE.md mục [1.4].
