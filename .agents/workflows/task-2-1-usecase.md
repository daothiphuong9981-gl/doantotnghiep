---
description: Task 2.1 - Use-case diagram & đặc tả
---

# Workflow: Task 2.1 — Use-case
### 1. Chuẩn bị
- Chạy `/mo-phien` trước (nạp ngữ cảnh, xác định vai, chờ tôi duyệt).
- Đóng vai ARCHITECT.

### 2. Thực hiện
- Danh sách actor + use-case, ánh xạ mỗi use-case về FR.
- Use-case diagram bằng Mermaid/PlantUML (render ra hình).
- Đặc tả 5 use-case chính (Đặt sân, CRUD sân, Xác nhận/hủy booking, Tìm kiếm, Quản trị): mã UC, actor, tiền điều kiện, luồng chính, luồng ngoại lệ, hậu điều kiện.
- Luồng ngoại lệ "Đặt sân" PHẢI có: slot đã bị đặt, mất kết nối, chưa đăng nhập.
### Kết thúc
- Chạy `/dong-phien` (cập nhật STATE, tick ROADMAP, ghi EVIDENCE, nhắc commit).

## Tiêu chí hoàn thành
Diagram render được + 5 đặc tả đủ luồng chính và ngoại lệ.
