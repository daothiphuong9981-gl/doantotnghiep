---
description: Task 6.3 - Slide & kịch bản demo
---

# Workflow: Task 6.3 — Slide & demo
### 1. Chuẩn bị
- Chạy `/mo-phien` trước (nạp ngữ cảnh, xác định vai, chờ tôi duyệt).
- Đóng vai DOCUMENTER.

### 2. Thực hiện
- Dàn ý slide 12–15 trang: giới thiệu → bài toán/mục tiêu → khảo sát/khả thi → thiết kế (ERD, kiến trúc) → chức năng (ảnh) → điểm nhấn kỹ thuật (chống trùng lịch, WebGIS) → kiểm thử → triển khai (link thật) → kết luận.
- Kịch bản demo 7–10 phút: thứ tự thao tác + thuyết minh + thời lượng.
- Phương án dự phòng: video demo + dữ liệu demo sạch.
### Kết thúc
- Chạy `/dong-phien` (cập nhật STATE, tick ROADMAP, ghi EVIDENCE, nhắc commit).

## Tiêu chí hoàn thành
Dàn ý slide + kịch bản demo có thời lượng + phương án dự phòng.
