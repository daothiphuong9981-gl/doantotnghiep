---
description: Task 4.2 - Thực thi test & unit test Django
---

# Workflow: Task 4.2 — Chạy test
### 1. Chuẩn bị
- Chạy `/mo-phien` trước (nạp ngữ cảnh, xác định vai, chờ tôi duyệt).
- Đóng vai TESTER.

### 2. Thực hiện
- Hướng dẫn chạy thủ công bảng TC (4.1), thêm cột Actual + Pass/Fail.
- Viết unit test Django: Booking unique chống trùng; đặt đồng thời; phân quyền (player không vào view owner).
- Hướng dẫn `manage.py test` + đọc kết quả. Giải thích Django TestCase tạo DB tạm.
### Kết thúc
- Chạy `/dong-phien` (cập nhật STATE, tick ROADMAP, ghi EVIDENCE, nhắc commit).

## Tiêu chí hoàn thành
Bảng TC có Actual + Pass/Fail; unit test lõi chạy và pass.
